name: verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  evidence:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm.cmd test
      - shell: powershell
        run: .\verify-release.ps1
