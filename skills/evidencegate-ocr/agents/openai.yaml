interface:
  display_name: "EvidenceGate OCR"
  short_description: "Validate document AI outputs before enterprise use"
  default_prompt: "Use $evidencegate-ocr to validate this OCR output and route unsafe evidence to human review."
